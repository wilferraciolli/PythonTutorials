import { Component } from '@angular/core';
import { MatIconModule } from '@angular/material/icon';

// Placeholder for the Cloudflare Workers AI tutorial project — no API
// integration yet. Exists so the home hub has a real route to link to
// while that project gets built.
@Component({
  selector: 'app-workers-ai-page',
  imports: [MatIconModule],
  templateUrl: './workers-ai-page.html',
  styleUrl: './workers-ai-page.scss',
})
export class WorkersAiPage {}
