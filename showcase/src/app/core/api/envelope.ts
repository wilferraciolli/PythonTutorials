// Superseded by @wiliamferraciolli/ngx-api-client (ApiEnvelope, ILink,
// SingleEnvelope, CollectionEnvelope, ApiClientService, LinkService,
// MetadataService — see current-user.store.ts, todos.store.ts,
// tags.store.ts). Nothing in the live app imports this file; only the
// still-orphaned resource-management-ui leftovers do (excluded from the
// build/test programs — see tsconfig.app.json/tsconfig.spec.json). Kept
// only until file deletion is unblocked — do not import from here.
export {};
