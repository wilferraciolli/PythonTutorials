services:
  api:
    build: .
    ports:
      - "8002:8002"
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_MODE: sqlite
      DATABASE_PATH: /data/local.db
    volumes:
      - ./src:/app/src
      - ./migrations:/app/migrations
      - sqlite-data:/data
    command:
      [
        "uv",
        "run",
        "uvicorn",
        "main:app",
        "--app-dir",
        "src",
        "--host",
        "0.0.0.0",
        "--port",
        "8002",
        "--reload",
      ]

volumes:
  sqlite-data:
