[project]
name = "fastapi-todo-d1"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "aiosqlite",
    "fastapi",
    "httpx",
    "uvicorn",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
