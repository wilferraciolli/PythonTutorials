[project]
name = "fastapi-template"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "aiosqlite",
    "fastapi",
    "httpx",
    "pyjwt[crypto]",
    "uvicorn",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
